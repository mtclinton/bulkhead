# SPDX-License-Identifier: AGPL-3.0-only
# Demo agent A: may reach the public internet (plus loopback + local IPC). Paired with
# agent B (loopback,other only) to prove per-agent egress: same destination, opposite
# verdict, decided solely by which cgroup's manifest applies. Remove these two demo
# instances when the real agent runtime lands.
[Service]
Environment=BULKHEAD_AGENT_EGRESS=public,loopback,other
